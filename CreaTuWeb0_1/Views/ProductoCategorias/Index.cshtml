@model IEnumerable<CreaTuWeb0_1.Models.ProductoCategoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Categoria.NombreCategoria)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Producto.NombreProducto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Categoria.NombreCategoria)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.NombreProducto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductoCategoriaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductoCategoriaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductoCategoriaId })
        </td>
    </tr>
}

</table>
